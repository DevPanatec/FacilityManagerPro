export const dynamicPages = {
  '/': true,
  '/shared/schedule': true,
  '/auth/login': true,
  // Agrega aquí todas las rutas que usan autenticación/cookies
}

export const dynamic = 'force-dynamic' 
